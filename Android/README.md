# ExtendCast for Android

The Android application contains both receiver and sender modes. It is an
independent Gradle project and can be opened directly in Android Studio.

```bash
cd Android
./gradlew :app:assembleDebug
```

The debug APK is written to `app/build/outputs/apk/debug/`.
